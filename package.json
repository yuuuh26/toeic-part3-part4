{
  "name": "toeic-listening-trainer",
  "version": "1.1.8",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "python3 scripts/validate.py",
    "serve": "python3 -m http.server 8000",
    "test": "node --check js/app.js && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "6.2.5"
  }
}
